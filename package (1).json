{
  "name": "rest-express",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist"
  },
  "dependencies": {
    "vite": "^5.4.14",
    "@replit/vite-plugin-cartographer": "^0.1.4"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  }
}